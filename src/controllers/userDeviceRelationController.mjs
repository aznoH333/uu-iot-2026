export const claimDevice = (req, res) => {
    res.status(501).send()
}

export const addUserToDevice = (req, res) => {
    res.status(501).send()
}

export const leaveDevice = (req, res) => {
    res.status(501).send()
}
